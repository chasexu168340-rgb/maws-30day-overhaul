{
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node maws_src/tools/build_check.mjs",
    "test:smoke": "playwright test maws_src/tests/phaser-smoke.spec.js --browser=chromium --reporter=line",
    "check": "npm run build",
    "check:full": "npm run build && npm run test:smoke"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
