# maws-agent-01-core

## Git State

- Branch: `feat/reforge-core-unlocks`
- Commit: `49b927c`
- Status: clean

## Contents Preserved

- `agent_reports/`: reports copied from this agent workspace.
- `content_proposals/`: proposal files if this agent produced any.
- `codex/`: DONE/HANDOFF/NEXT_PROMPT snapshots if present.
- `docs__TASK_PLAN.md`: this agent workspace checkpoint if present.

## Note

The full worktree copy is kept locally under `maws-agent-01-core/` but is intentionally not tracked in Git to avoid duplicating the whole repository inside itself.
