# TASK_PLAN

> Current task sheet. Keep only this task, result, validation, risks, and next step.

## Current Task

SkillOpt-style first-look visual/UI optimization for the current playable slice.

## Scope

- Apply a bounded audit -> patch -> validate loop to the first screen players see.
- Improve player-facing polish without changing gameplay systems, starter skills, combat formulas, economy, save keys, or asset structure.
- No CLI workers were started. No images were generated by workers.

## Current Result

- Audited current 1365x768, 390x844, and E00 combat screenshots under local `outputs/`.
- Chose the highest-impact low-risk gap: the boot screen still looked like a development shell because it exposed `PHASER UI` and oversized debug-like title treatment before gameplay.
- Removed player-facing `PHASER UI` wording from the boot screen and browser title.
- Rewrote boot subtitle into in-world game language: the player starts from the rented room, convenience store, and boxing gym path toward "去伪存真".
- Replaced the production-style title badge with the player-facing `30日试炼` badge.
- Tightened title-card spacing, mobile title scaling, and boot-screen visual hierarchy while keeping the existing red/black/gold visual language.
- Added a Playwright smoke guard that verifies the boot screen is visible, does not contain `PHASER UI`, has the production title, and does not overflow at 390x844.

## Validation

- Passed:
  - `npx playwright test maws_src/tests/phaser-smoke.spec.js --browser=chromium --reporter=line`
  - `npm run check:full`
  - `npm run test:playtest`
  - `git diff --check`

## Risks

- This is a first-look polish pass, not a full visual overhaul.
- Generated screenshots under `outputs/` are local audit artifacts and must remain uncommitted.
- The boot screen is improved but the larger goal still needs staged work on pixel-art asset quality, scene VFX, event text pass, and full UI consistency.

## Next Step

Commit and push this first-look boot-screen polish pass on `codex/skillopt-optimization-pass`.
