# TASK_PLAN

> Current task sheet. Keep only this task, result, validation, risks, and next step.

## Current Task

SkillOpt-style in-game first-scene visual/UI optimization for the current playable slice.

## Scope

- Continue the audit -> patch -> validate loop from the boot screen into the first playable Day 1 rental-room scene.
- Improve the first in-game impression, mobile readability, and NPC click feedback without changing systems, combat formulas, economy, starter skills, save keys, or asset structure.
- No CLI workers were started. No images were generated by workers.

## Current Result

- Captured current Day 1 rental-room screenshots at 1365x768 and 390x844 plus NPC-click state under local `outputs/`.
- Identified the highest-impact in-game first-look issue: mobile HUD, scene title, toast, characters, and bottom nav were competing for the same vertical space.
- Compressed the mobile HUD and bottom nav so the bottom navigation stays fully inside the 390x844 viewport.
- Reworked mobile scene info into a shorter location strip and moved the opening toast to the lower area so it no longer covers the location title.
- Adjusted mobile scene/cast spacing so the player, Liu Pangzi, and father marker read as part of the room before the action boards.
- Replaced native-looking scene-character focus feedback with explicit scene feedback: no visible native focus square, plus a ground-light click response.
- Strengthened `wave13_first_look.spec.js` to assert the bottom nav stays inside the mobile viewport, the opening toast stays inside the viewport, the main CTA remains reachable above the nav, and scene character clicks do not show native focus-square styling.

## Validation

- Passed:
  - `npx playwright test maws_src/tests/wave13_first_look.spec.js --browser=chromium --reporter=line`
  - `npm run check:full`
  - `npm run test:playtest`
  - `git diff --check`

## Risks

- This improves the first playable scene layout and click feedback, but it is not a full art/VFX replacement pass.
- Generated screenshots under `outputs/` are local audit artifacts and must remain uncommitted.
- Larger goal still needs staged work on pixel-art asset quality, scene VFX, event prose, and UI consistency across later days and battle results.

## Next Step

Commit and push this Day 1 first-scene polish pass on `codex/skillopt-optimization-pass`.
